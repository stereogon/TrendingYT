<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Trends API</title>
        <link rel="stylesheet" href="../css/styles.css" />
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700;900&display=swap"
            rel="stylesheet"
        />
    </head>
    <body>
        <%- include('./partials/navbar.ejs') %>
        <div class="container2">
            <h1>API Endpoints</h1>
            <div class="endpoints-container">
                <div class="ep-container">
                    <h3 class="endpoint"><span>GET</span> /trendingdb</h3>
                    <p>
                        Fetches trending page data from the database.
                    </p>
                </div>
                <div class="ep-container">
                    <h3 class="endpoint"><span>POST</span> /trending</h3>
                    <p>
                        Scraps the trending page and stores the data into the database, redirects to /trendingdb
                    </p>
                </div>
                <div class="ep-container">
                    <h3 class="endpoint"><span>GET</span> /trendingdb/videoID</h3>
                    <p>
                        Opens the specified video in a new window, autoplays it (while muted)
                    </p>
                </div>
            </div>
        </div>
        <%- include('./partials/footer.ejs') %>
    </body>
</html>
